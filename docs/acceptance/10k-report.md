# 一万份资料库性能与验收报告

- 日期：2026-09-13
- 分支：`codex/complete-remaining-features`
- 验收起点：`116e358`
- 结论：工作单 12 的自动化验收通过。10,000 份文档完成创建、导入、索引、重启、搜索、筛选、外部修改、回收站恢复和永久删除闭环，未发现未解决的阻塞、数据安全或高影响性能问题。

## 测试环境

| 项目 | 实际值 |
| --- | --- |
| 操作系统 | Microsoft Windows 11 家庭版 中文版，版本 10.0.26200，64 位 |
| 设备 | LENOVO 82WK |
| CPU | 13th Gen Intel Core i9-13900HX，24 核 / 32 线程 |
| 内存 | 15.81 GB |
| Rust | rustc 1.98.1，cargo 1.98.1 |
| Node.js | v24.15.0 |
| npm | 11.12.1 |
| 构建配置 | debug 测试配置；真实临时目录、真实 SQLite/FTS5、真实文件复制与哈希 |

测试资料库与合成源文件均位于系统临时目录。10k 命令结束后由 `tempfile` 清理测试目录，不保留测试数据库作为项目交付物。

## 样本与覆盖

自动化工具位于 `src-tauri/tests/ten_thousand_acceptance.rs`，包装命令为 `scripts/run-10k-acceptance.ps1`。

样本为固定且可复现的 10,000 份文档：

| 格式 | 数量 |
| --- | ---: |
| PDF | 1,667 |
| DOCX | 1,667 |
| TXT | 1,667 |
| Markdown | 1,667 |
| JPG | 1,666 |
| PNG | 1,666 |

每份文件包含唯一正文或唯一图片字节，避免把内容重复决策混入导入性能结果。正文格式覆盖中文标记、英文标记、短元数据词和组合筛选元数据：

| 验收集合 | 数量 |
| --- | ---: |
| 中文正文命中 | 1,333 |
| 英文正文命中 | 953 |
| 两字符短查询命中 | 50 |
| 集合 + 标签 + 类型 + 日期组合筛选命中 | 50 |
| 外部修改文档 | 121（120 份成功，1 份损坏失败） |
| 移入回收站 | 100 |
| 恢复 | 50 |
| 永久删除 | 50 |

执行命令：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\run-10k-acceptance.ps1
```

最近一次原始输出保存在 `target/acceptance/10k-latest.log`。

## 关键结果

| 阶段或验收项 | 结果 | 实测耗时 |
| --- | --- | ---: |
| 生成 10,000 份合成源文件 | 通过 | 3,057 ms |
| 首次批量导入 | 10,000/10,000 成功，20,002 个进度事件，序号单调 | 62,682 ms |
| 首次全文索引 | 10,000/10,000 可搜索，0 失败 | 62,554 ms |
| 关闭后重新打开 | 文档、集合、标签和索引状态一致，并按内容哈希完成一致性核对 | 3,979 ms |
| 10,000 条列表读取 | 10,000 条完整摘要 | 67 ms |
| 中文正文查询 | 1,333 条，均带正文匹配片段 | 100 ms |
| 英文正文查询 | 953 条，均带正文匹配片段 | 104 ms |
| 两字符短查询 | 50 条，走元数据匹配 | 92 ms |
| 组合筛选 | 50 条，集合/标签/类型/日期全部生效 | 10 ms |
| 外部修改扫描 | 内容哈希一致性检查发现 121 份变化 | 4,210 ms |
| 外部修改重新索引 | 120 份可搜索，1 份损坏明确失败 | 1,183 ms |
| 回收站、恢复、永久删除 | 100 份入回收站，50 份恢复，50 份永久删除 | 943 ms |
| 源文件不变检查 | 10,000/10,000 字节级一致 | 1,175 ms |
| 10k 验收总耗时 | 全部断言通过 | 151,060 ms |

最终活动文档数为 9,950：10,000 份减去 100 份回收站操作，再加回 50 份恢复；永久删除的 50 份资料库副本均已移除。标签计数按仍在活动列表中的已整理文档重新计算并校验。

## 端到端冒烟

后端闭环通过真实 `LibraryService`、真实临时资料库和真实 SQLite 自动执行：

1. 创建资料库。
2. 导入覆盖六种支持格式的一万份文档。
3. 创建集合和标签，批量移动、批量加标签并更新文档元数据。
4. 执行中文、英文、短查询和组合筛选。
5. 读取 TXT 文档预览。
6. 外部修改 121 份资料库副本，其中 1 份构造为损坏 PDF。
7. 重启后执行增量扫描和重新索引，验证成功与失败项。
8. 将 100 份文档移入回收站，恢复 50 份，永久删除 50 份。
9. 关闭服务并移出最近列表，确认资料库目录及其数据不被删除。
10. 校验全部源文件字节未发生变化。

真实 GUI 未使用原生指针或 Tauri WebDriver 驱动执行。当前项目未配置 Playwright、WebDriver 或桌面端截图基线；因此本轮不声称完成原生 GUI 自动化。React 测试覆盖一万份数据下的初始 DOM 窗口、滚动增量加载、网格切换和索引进度显示，后端闭环不因此跳过。

## 发现并修复的问题

| 影响 | 问题 | 修复 |
| --- | --- | --- |
| 高 | 索引循环反复扫描并排序剩余待处理文档，10k 规模接近二次复杂度 | 增加活动导入顺序索引和待处理队列索引 |
| 高 | 列表、搜索和回收站为每份文档单独查询标签，10k 规模产生上万次 SQL | 使用一次批量 CTE 查询装配全部文档标签 |
| 高 | 列表和网格一次渲染全部 10k 行/卡片，且逐项线性搜索完整搜索结果 | 首批最多渲染 200 项，滚动邻近底部时每批增加 200 项；搜索结果与集合改为 `Map` 查找 |
| 中 | 索引阶段只有旋转提示，没有已处理数量 | 后端每 100 份发送一次处理进度事件，前端显示 `processed/total`；实际索引仍在阻塞任务中执行，不占住 UI 事件循环 |
| 高 | 永久删除先执行 DB 删除再删除资料库副本；提交失败或进程中断可能丢失副本 | 先在同一文档目录重命名为 tombstone，再提交 DB；失败立即恢复，成功后清理 tombstone，打开资料库时按 DB 状态恢复或清删遗留 tombstone |
| 高 | 清空回收站遇到单项失败会整体中止，UI 无法知道哪些项已删、哪些仍可重试 | 后端逐项返回成功/失败；前端显示部分失败明细并保持对话框打开，失败项继续留在回收站供重试 |
| 高 | 启动/打开一致性检查仅在大小或时间戳变化时计算哈希，同大小同时间戳改写会漏检 | 打开资料库时对全部活动副本执行内容哈希核对；运行期仍使用可合并的轻量指纹监视 |
| 中 | 索引进度 total 来自导入前文档快照，导入后可能显示 `0/0`；无 `item` 或无 `result` 的事件不更新文档状态 | 新增后端实时待索引计数；前端用 `documentIds`/进度事件立即显示“处理中”，完成后刷新真实文档状态 |

## 回归测试

- `src-tauri/tests/ten_thousand_acceptance.rs`：固定生成并验收全部支持格式的一万份文档，覆盖导入进度、首次索引、全量哈希重启核对、查询、筛选、外部修改、回收站和源文件安全。
- `src-tauri/tests/library_service.rs`：新增数据库删除失败后副本恢复、两类崩溃遗留 tombstone 对账、同大小同时间戳改写检测、部分清空失败继续处理，以及监视器释放回归。
- `src/ScalePerformance.test.tsx`：验证 10k 数据下列表初始只创建 200 行，滚动后增量展开，网格初始只创建 200 张卡片，并验证真实索引进度计数显示。
- `src/ImportFlow.test.tsx`、`src/ExternalChange.test.tsx`、`src/Trash.test.tsx`：覆盖无 `item` 导入进度、实时 pending total、仅带文档 ID 的处理中事件、完成后刷新和清空回收站部分失败重试。
- 原有 `LibraryService` 集成测试继续覆盖导入失败、重试、标签生命周期、搜索、外部变化、回收站和永久删除。

## 限制与后续事项

- 首次导入和首次索引在 debug 构建下各约 62 秒。界面通过分项进度保持可响应，但本轮没有测量 release 构建耗时，也没有给出用户可感知的取消索引能力。
- 全量哈希启动核对将 10,000 份文档的重启检查增至约 4 秒。该耗时用于保证同大小且时间戳不变的资料库副本改写不会漏检，属于当前数据安全取舍。
- 本轮将 DOM 首屏限制在 200 项并验证滚动增量加载，但没有执行长时间内存采样或数小时 soak test；因此可以确认初始 DOM/状态规模受控，不能据此宣称任何操作序列下内存曲线绝对平坦。
- PDF、DOCX 使用可解析的最小合成文件，足以覆盖格式识别、正文提取和索引路径，但不代表复杂真实文档的解析性能。
- 原生 GUI、鼠标、键盘和截图级视觉验收未自动化。若后续引入 Tauri WebDriver/Playwright，应复用同一 10k 资料库增加窗口级滚动和内存验证。

## 判定

工作单 12 的自动化验收标准通过。高影响索引、标签查询、前端 10k 渲染、索引进度、永久删除安全、清空回收站部分失败和外部变化漏检问题均已修复并有回归覆盖；剩余事项均为非阻塞的测试基础设施或 soak 测试限制。
