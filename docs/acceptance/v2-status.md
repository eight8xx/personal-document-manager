# 第二版验收状态报告

- 日期：2026-09-23
- 分支：`codex/v2-development`
- 基线起点：`dcc5cd8`（第二版已完成 01/02/06 的状态）
- 本报告对应 HEAD：自动化全绿状态（Rust 177 项 / 前端 159 项），后续提交会继续推进，见文末「未完成项」
- 结论：**工作单 03、04、05、06、08、09、10、11、12、13 已完成并合并；07 因环境原因暂停；14 的自动化部分完成、真实安装与真实样本验收未做。** 本报告只记录有证据的结论，未验证部分一律标注为未完成。

## 一、交付与验证总览

| 工作单 | 状态 | 证据 |
| --- | --- | --- |
| 01 资料库切换一致性 | 已完成（基线内 `71bcb19`） | 服务层与命令层双库回归；前端跨库事件用例 |
| 02 待决导入的库边界 | 已完成（基线内 `71bcb19`） | 双库集成测试；待决项在另一库被拒绝 |
| 03 替换副本中断恢复 | 已完成 | `d2c7bc7`；`tests/replacement_recovery.rs` 10 个用例 |
| 04 文档资源上限 | 已完成 | `0319b3b`；`library/limits.rs` 9 个单元用例 + `library_service.rs` 新增 9 个集成用例 |
| 05 导入期间读取响应 | 已完成 | `3e517d4`（后端「每项让锁」）+ `b70e826`（界面 3 个用例） |
| 06 结果窗口化 | 已完成（基线内 `6061c21`） | `ScalePerformance.test.tsx` 规模用例 |
| 07 原生窗口冒烟 | **暂停** | 见第四节 |
| 08 CSV 表格文档 | 已完成 | `e559bbe` + `b532671` + `6aa48ae`；`table_formats.rs` 23 例、`table_documents.rs` 5 例 |
| 09 XLSX 表格文档 | 同上 | 同上（共享同一解析层与端到端用例） |
| 10 分类规则 | 已完成 | `8f643a6`（后端 `plan_classification`）+ `14f71ec`（规则面板）+ `25d61bd`（设置接线）+ `71e1577`（导入前询问、变化时复核）；7/7 验收勾选 |
| 11 每库接收目录与启动补扫 | 已完成 | `8f643a6` + `09781d4`（Windows 共享冲突真实样本）；`receive_sources.rs` 18 例、`receive_locked_files.rs` 2 例；7/7 |
| 12 QQ/微信双来源 | 已完成 | `8f643a6`；候选目录带识别依据、每库独立启停、失效不静默换目录；7/7 |
| 13 实时接收导入 | 已完成 | `8f643a6`（周期补扫 + 每文件加锁 + 逐项核对库身份 + 指纹去重 + 失败冷却）+ `71e1577`（事件监听与导入日志区）；8/8 |
| 14 release 一万份与真实文件验收 | 部分完成 | 自动化与 release 产物已在本报告第二节给出；真实安装/运行、真实样本与 QQ/微信实配未做 |

## 二、自动化测试现状（本机实测）

| 套件 | 结果 | 命令 |
| --- | --- | --- |
| Rust 全部 | **202 passed / 0 failed / 1 ignored** | `cargo test --manifest-path src-tauri\Cargo.toml` |
| ├ 单元（含 limits/store/formats/commands 契约与监视退出） | 46 passed | 同上 |
| ├ `library_service.rs` 集成 | 75 passed | 同上 |
| ├ `receive_locked_files.rs`（11 文件占用） | 2 passed | 同上 |
| ├ `receive_sources.rs`（11/12/13 接收目录 + M3） | 23 passed | 同上 |
| ├ `reconcile_isolation.rs`（M4 恢复隔离） | 3 passed | 同上 |
| ├ `recovery_orphans.rs`（L2 孤儿副本） | 2 passed | 同上 |
| ├ `replacement_recovery.rs`（03） | 10 passed | 同上 |
| ├ `sparse_column_amplification.rs`（H1 独立复现） | 2 passed | 同上 |
| ├ `table_documents.rs`（08/09 端到端） | 13 passed | 同上 |
| └ `table_formats.rs`（08/09 解析层 + H1/L3） | 26 passed | 同上 |
| 前端全部 | **172 passed（21 文件）** | `npm test` |
| 类型检查 | 通过 | `npm run typecheck` |
| 前端生产构建 | 通过 | `npm run build` |

`ten_thousand_acceptance.rs` 需要显式脚本运行（`scripts/run-10k-acceptance.ps1`），不在默认套件内。

### 一万份资料库（本版代码上的复测）

`powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\run-10k-acceptance.ps1` → **1 passed**，总耗时 **334.86 秒**（debug 测试配置，与第一版同类环境下的记录不可直接比较）。关键指标：

| 指标 | 本版实测 |
| --- | --- |
| 生成 10,000 份合成源文件 | 4,843 ms |
| 首次批量导入 | 123,239 ms（10,000/10,000，20,002 个进度事件） |
| 首次全文索引 | 120,225 ms（10,000/10,000） |
| 关闭后重开一致性核对 | 13,410 ms |
| 10,000 条列表读取 | 58 ms |
| 中文 / 英文正文查询 | 406 ms / 473 ms（各 1,333 / 953 命中） |
| 两字符短查询 / 组合筛选 | 267 ms / 29 ms（50 / 50 命中） |
| 外部修改扫描与重新索引 | 15,804 ms / 1,871 ms（121 份变化，120 成功、1 份损坏明确失败） |
| 回收站、恢复、永久删除 | 1,548 ms（100 入回收站、50 恢复、50 永久删除） |
| 源文件不变核对 | 6,089 ms（10,000/10,000 字节级一致） |
| 最终活动文档数 | 9,950 |

导入与索引耗时高于第一版记录的 62/63 秒，属于 debug 构建下的波动范围（同环境第一版最终复测为 87/86 秒），**不能据此判定 release 性能**；release 性能仍属工作单 14 未完成项。

### 发布产物（本版源码重新构建）

`npm run package:windows` → 成功（`cargo build --release` 3 分 48 秒）：

| 产物 | 大小 |
| --- | --- |
| `release/个人文档管理-Setup-0.1.0.exe` | 209.5 MB（内含 WebView2 离线组件） |
| `release/个人文档管理-Portable.exe` | 14 MB |

产物已按合并后的最终源码重建。**未做的是真实安装/运行验证**：安装向导、快捷方式、首次启动与真实文档操作仍属工作单 14。

## 三、本版新增的关键设计

- **契约先行**：格式能力表（`shared/document-format-capabilities.json`）扩到 9 种格式（新增 CSV、XLSX），前后端共用；表格预览、分类规则、接收目录三份契约在动手前冻结，两端并行实现后一次合并通过。
- **表格文档**：`library/table.rs` 纯解析层（零新依赖，复用最小 ZIP 读取器 + flate2），支持多工作表、共享字符串、稀疏行（保留真实 Excel 行号 `<code>row_numbers</code>`）、公式只读缓存值、CSV 引号/换行/BOM；拒绝旧 XLS、加密容器、ODS、XLSM、XLSB 并给出具体原因。
- **资源上限**：`library/limits.rs` 统一输入大小、单条目声明、压缩比、累计解压量与表格行列上限；DOCX/PPTX/XLSX 的 ZIP 读取在解压前校验；正文提取有字符上限并显式标注截断。
- **导入让锁**：批量导入从「整批一把锁」改为「每项一把锁」，读取（列表/搜索/预览）不再排队到整批结束；每项执行前重新核对资料库身份，中途切库时该批以 `invalidLibrary` 结束，已完成项留在原库、新库零写入。
- **资料库级数据**：分类规则、接收来源、用户明确跳过的源文件、接收导入日志都存进资料库 SQLite（`library/store.rs`），随资料库迁移，不写应用级状态。

## 四、工作单 07 的暂停说明

07 剩余的唯一验收条目要求自动化驱动**真实原生文件选择器 / 首次向导 / 导入按钮**。在当前环境下这需要在用户日常使用的交互式桌面上注入鼠标与键盘输入（tauri-driver + WebDriver，或 Win32 消息驱动原生对话框）。本轮尝试时占用了用户桌面，已被用户要求停止，脚本未提交并已删除，过程记录在 `issues/07-native-smoke-test.md`。

当前可复现的部分：`npm run test:native-smoke` 仍能在真实 Windows 窗口中跑通 9/9 步（含 TXT 导入后的集合整理、搜索、预览、回收站与恢复），覆盖标志 `nativeFilePickerCovered`、`libraryWizardCovered`、`importButtonCovered` 均为 `false`，不得据此签收。剩余部分需要：① 人工按文档执行并记录；② 在独立测试机/虚拟机执行；或 ③ 明确接受该条长期未覆盖。

## 五、人工验收（用户，2026-09-23）

用户在 release 产物上用自己的**真实文档**（含 PDF/DOCX/PPTX/XLSX/CSV）执行了两项验收，**两项均通过、未发现问题**：

| 验收项 | 结果 | 覆盖的内容 |
| --- | --- | --- |
| release 程序日常闭环 | 通过 | 安装/启动后完成导入、整理、搜索、预览、回收站等操作 |
| 表格文档 | 通过 | 导入真实 CSV/XLSX、切换工作表、按行列翻页预览、按单元格文字搜索命中 |

本轮**未覆盖**（保持开放）：接收目录（QQ/微信）配置与自动导入、「关闭期间新增文件重开补导入」、导入前的分类规则询问、资料库切换、替换副本中断重开、release 构建的 10k 性能与长时间滚动内存观察。

**被测二进制与当前源码的关系**：用户测的是 19:57 构建的产物。此后主分支又合入了 M4（中断恢复隔离）、L1（监视线程退出）、L2（孤儿副本隔离）、L3（稀疏表跳行）、M3（待决项界面出口）等修复，`release/` 下的产物已在 20:28 用最终源码重建。也就是说：上面两条通过的结论对「同源功能」成立，但没有覆盖那五项后加的修复；如需对最终二进制复测，建议至少再走一遍表格文档与日常闭环。

## 六、已知边界（有意保留，未假装解决）

- **单文件粒度**：让步粒度是「每项」，单个超大文件处理期间读取仍会等这一项，不会等整批。
- **PDF 侧解压上限**：04 只覆盖 DOCX/PPTX/XLSX 的压缩包读取；`extract_pdf_text` 与 `pdf_security.rs` 的流解码仍无统一上限，需要时另开工作单。
- **索引截断无独立状态**：正文超限截断通过预览降级说明与统计体现，`DocumentSummary` 未增加字段。
- **表格预览取舍**：非活动工作表的行列数返回 `None`（不为切表扫描整本工作簿）；CSV 每页请求会整文件扫描一次以给出精确总行数。
- **监视是周期轮询而不是文件系统事件**：仓库没有 `notify` 依赖，接收目录监视每 2 秒补扫一次，靠「事件漏了也能发现」而不是即时事件；未自行新增依赖。
- **失败项冷却**：周期补扫对失败项有 60 秒冷却，用户主动点「扫描」会立即重试；这是有意区分，避免每 2 秒刷一次日志。
- **导入期输入上限对 CSV 也已覆盖**：`validate_file_content` 的预检包含 `CsvText`，超限 CSV 与 DOCX/PPTX/XLSX 一样在校验阶段单项失败；同时避免结构校验先把整份超大文件读进内存。
- **两字符查询**：按设计只匹配标题/描述/元数据，不匹配正文（FTS5 trigram 的既有取舍）。
- **测试配置**：`vite.config.ts` 已排除 `.scratch`，避免把其它工作区副本的测试当成本仓库测试；并行构建负载下用例超时放宽到 15s、`findBy*` 等待放宽到 3s（断言未放宽）。
- **原生窗口自动化**：见第四节，07 未覆盖的原生交互不接受「用假后端替代」或「改个名字算覆盖」。
- **表格解析的远列引用**：XLSX 单元格列引用超过 Excel 上限 XFD（16384 列）会被拒绝；索引路径按「只取值不补位」处理并用单元格预算约束保留量。修复前 3 行 / 1.6 KB 的远列样本会让索引提取耗时 3.59 秒，现在 0.01 秒。
- **残留清理失败的用户可见性**：若 `documents/<id>/` 下有一个删不掉的残留（被占用、只读属性等），应用会跳过它继续打开资料库，文档保持可用，每次打开重试；但界面**不会提示**存在未清理残留（无资料库级警告通道）。
- **孤儿副本的隔离区**：崩溃后「副本已落盘、数据库未写入」留下的孤儿目录，打开资料库时会被整体移到 `<资料库>/.recovered-orphans/<id>/`（**只搬不删**，附 `orphan-copy-report.txt` 说明如何还原）；只搬目录内最新 mtime 超过 10 分钟的，避免打断另一实例正在进行的导入。诊断同样只在磁盘说明文件上，界面无提示。
- **代码审查待办**：`docs/acceptance/v2-review.md` 列出的问题中，H1/H2/M4 已修复并验证，L1/L2/L3 中 L1/L2 已修复、L3 后端已交付（前端在 task-20），M3 已扩展契约（后端 task-18、前端已交付）。

## 七、未完成项

- [ ] 工作单 07 的原生交互验收（见第四节）。
- [ ] 工作单 14 剩余部分：接收目录（QQ/微信）配置与自动导入、关闭期间新增文件补导入、导入前分类询问、资料库切换、替换副本中断重开、release 构建的 10k 性能与长时间滚动内存观察、对最终二进制的复测。
- [ ] 工作单 14 已完成的部分：release 程序日常闭环与真实 CSV/XLSX 表格文档（见第五节）。
- [ ] 发布产物：`release/` 下的安装包与便携版需重新生成（`npm run package:windows`），当前工作副本内不存在。
