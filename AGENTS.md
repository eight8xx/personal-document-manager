## Agent skills

### Issue tracker

本地 Markdown：需求和 PRD 位于 `.scratch/`。参见 `docs/agents/issue-tracker.md`。

### Triage labels

使用五个默认分类标签。参见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文：根目录 `CONTEXT.md`，架构决策位于 `docs/adr/`。参见 `docs/agents/domain.md`。
