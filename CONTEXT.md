# 个人文档管理

一个本地优先的个人文档管理应用，用于将文件导入受管理的资料库，并通过元数据、分类和检索重新组织这些文档。

## 语言

**源文件**：
位于资料库外、作为导入来源的本地文件。源文件可以由用户选择或从接收目录发现，不会被应用修改。
_避免_：原文件、原始文档、导入文件

**资料库副本**：
应用导入并持续管理的文件副本。导入完成后，资料库副本是权威版本。
_避免_：托管文件、导入副本、应用文件

**来源信息**：
记录文档所对应源文件的路径、标识和最近导入时间。来源信息不表示源文件与资料库副本保持同步。
_避免_：同步关系、源文件链接、绑定信息

**接收目录**：
QQ 或微信在这台电脑上用于保存文件的本地目录。接收目录位于资料库之外，其中的文件仍是源文件；仅凭目录不能判断文件的发送者或会话。
_避免_：聊天记录目录、同步目录

**自动导入**：
应用发现接收目录中新保存的源文件后，无需用户逐个选择文件而执行的导入。自动导入不读取聊天记录或尚未保存到本机的附件。
_避免_：聊天同步、附件同步

**资料库**：
由应用管理的文件集合，包含所有资料库副本及其元数据。
_避免_：文件仓库、存储区、库

**文档**：
用户在应用中整理、检索和打开的对象，由一个资料库副本及其元数据组成。
_避免_：文件、条目、资源

**PPTX 文档**：
以 `.pptx` 扩展名提供的幻灯片文档，可以导入、只读预览、提取正文和再次外部打开。第一版不包含旧 `.ppt` 或启用宏的 `.pptm`。
_避免_：PPT、PowerPoint 文件

**表格文档**：
以 XLSX 或 CSV 格式提供、内容按行列组织的文档。第二版暂不包含旧 XLS 或 ODS 格式。
_避免_：Excel 文件、表格文件

**导入**：
将源文件复制到资料库并创建文档的操作。
_避免_：上传、添加、扫描

**导入批次**：
一次批量导入操作，包含用户选择的文件和文件夹扫描出的全部文件。
_避免_：导入任务、批次任务

**导入项**：
导入批次中单个文件的处理记录，记录其来源、状态、最终结果和失败原因。
_避免_：导入条目、导入单元

**集合**：
文档的主要归档位置。集合可以嵌套，每份文档归属于一个集合。
_避免_：文件夹、目录、分组

**收件箱**：
新导入文档默认进入的内置集合，用于在用户分配正式归档位置前暂存文档。
_避免_：待办箱、导入区、未分类文件夹

**标签**：
用于跨集合描述文档主题或属性的分类标记。一份文档可以拥有多个标签。
_避免_：关键词、类别、标记

**分类规则**：
用户定义的条件与归档结果，根据源文件名、类型或来源目录，为导入的文档指定集合和标签。分类规则只改变文档元数据，不修改源文件或资料库副本。
_避免_：智能分类、AI 分类

**文档日期**：
与文档内容相关、由用户维护的日期，可以与文件的创建时间或修改时间不同。
_避免_：文件日期、创建日期、修改日期

**版式预览**：
文档在应用内按原始类型的布局结构呈现的只读预览，包括段落、表格、图片和页面或幻灯片结构。版式预览不保证与外部办公软件像素级一致，也不表示文档可以编辑。
_避免_：完整预览、Office 预览、可编辑预览

**安全预览**：
预览文档时只读取资料库副本中的本地内容，不执行宏、脚本、嵌入对象、远程资源请求或自动媒体播放，也不修改资料库副本和源文件。
_避免_：沙箱预览、只读预览

**回收站**：
保存已删除但尚未永久移除的文档的暂存区域，其中的文档可以恢复。
_避免_：垃圾箱、废纸篓

**永久删除**：
从资料库中彻底移除文档及其资料库副本，且无法通过应用恢复的操作。
_避免_：彻底清除、销毁
